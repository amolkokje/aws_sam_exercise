def lambda_handler(event, context):
    print 'event={}'.format(event)
    print 'context={}'.format(context)


def lambda_handler_one(event, context):
    print 'event_one={}'.format(event)
    print 'context_one={}'.format(context)